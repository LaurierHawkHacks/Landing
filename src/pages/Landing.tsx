import { FooterSection } from '@components';
import { Navbar } from '@components';

const Landing: React.FC = () => {
    return (
        <div>
            <Navbar />
            <FooterSection />
        </div>
    );
};

export { Landing };
