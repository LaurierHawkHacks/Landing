import { Navbar, HeroSection, FooterSection } from '@components';

const Landing: React.FC = () => {
    return (
        <div>
            <Navbar />
            <HeroSection />
            <FooterSection />
        </div>
    );
};

export { Landing };
